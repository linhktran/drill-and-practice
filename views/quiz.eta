<% layout("./layouts/layout.eta") %>

<h1>Available topics for quiz</h1>

<ul>
    <% it.topics.forEach(topic => { %>
        <li><a href="/quiz/<%= topic.id %>"><%= topic.name %></a></li>
    <% }); %>
</ul>
